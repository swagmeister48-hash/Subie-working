// Intentionally empty: this app uses plain CSS, no PostCSS plugins.
// Having this file stops PostCSS from searching parent folders
// (which would pick up the old project's Tailwind config).
const config = {
  plugins: {},
};

export default config;
